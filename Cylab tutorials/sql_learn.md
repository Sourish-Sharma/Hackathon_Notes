## SQLi questions on picoCTF

- *(1) SQL direct:* Enter through the details provided, then run \dt to show all tables and the print the table using `SELECT * FROM flags;` 

- *(2) SQL Map1:* We are cooked, need to learn union injection properly with cheatsheets for different types of databases