- you can change name and email locally in git, and always check the branch with `git branch` before pushing to it through `git branch origin master/main`

- If you get a .db, check it's type, it's most probably a sqlite file which you can then run `sqlite3`